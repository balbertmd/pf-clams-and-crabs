---
import { getSite } from '../lib/site';
import { SITE, SHOW_QUOTES } from '../lib/config';
import SocialIcon from './SocialIcon.astro';
const site = await getSite();
const links = site.socials.filter((s) => s.show);
const tel = "tel:" + site.phoneHref;
---
<header class="site-header" data-lat={SITE.lat} data-lng={SITE.lng} data-tz={SITE.timezone}>
  <div class="container header-inner">
    <a href="/" class="brand"><img class="brand-logo" src="/images/logo.png" alt={site.name} /></a>
    <a class="header-phone" href={tel}><svg class="ico-tel" viewBox="0 0 24 24" aria-hidden="true" width="18" height="18" style="vertical-align:-.18em;margin-right:.35em"><path fill="currentColor" d="M6.6 10.9a15.5 15.5 0 0 0 6.5 6.5l2.2-2.2a1 1 0 0 1 1-.25 11.4 11.4 0 0 0 3.6.57 1 1 0 0 1 1 1V20a1 1 0 0 1-1 1A17 17 0 0 1 3 4a1 1 0 0 1 1-1h3.5a1 1 0 0 1 1 1 11.4 11.4 0 0 0 .57 3.6 1 1 0 0 1-.25 1z"/></svg>{site.phone}</a>
    <nav class="nav" id="site-nav">
      {/* Mirrors the homepage nav EXACTLY (labels and anchors). public/index.html is the front door
          and the only page that owns these ids; /#about and /#contact never existed there, so the
          404's "Meet Paul" and "Contact" silently dumped visitors at the top of the homepage. */}
      <a href="/#catch">The Catch</a>
      <a href="/#paul">Meet Paul</a>
      <a href="/#gallery">The Dock</a>
      <a href="/#hours">Find Us</a>
      <div class="nav-follow">
        <span class="nav-follow-label">Follow Us</span>
        <div class="nav-follow-links">
          {links.map((s) => (
            <a class="social-link" data-brand={s.brand} href={s.url} target="_blank" rel="noopener" aria-label={s.name}>
              <SocialIcon brand={s.brand} icon={s.icon} /><span class="social-name">{s.name}</span>
            </a>
          ))}
        </div>
      </div>
    </nav>
    <div class="header-actions">
      <div class="header-socials">
        {links.map((s) => (
          <a class="social-link" data-brand={s.brand} href={s.url} target="_blank" rel="noopener" aria-label={s.name}>
            <SocialIcon brand={s.brand} icon={s.icon} /><span class="social-name">{s.name}</span>
          </a>
        ))}
      </div>
      <button class="theme-toggle" data-theme-toggle aria-label="Toggle dark mode">
        <span class="tt-ico tt-sun" aria-hidden="true">☀️</span><span class="tt-ico tt-moon" aria-hidden="true">🌙</span>
      </button>
      <button class="nav-toggle" data-nav-toggle aria-label="Open menu" aria-expanded="false" aria-controls="site-nav">
        <span></span><span></span><span></span>
      </button>
    </div>
  </div>
</header>
